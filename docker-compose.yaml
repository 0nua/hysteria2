services:
  hysteria2:
    image: tobyxdd/hysteria:latest
    container_name: hysteria2
    restart: always
    network_mode: host
    privileged: true
    volumes:
      - $PWD/config.yaml:/etc/hysteria/config.yaml
      - /cert:/cert
    command: server -c /etc/hysteria/config.yaml
